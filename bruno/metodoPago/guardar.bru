meta {
  name: Guardar Metodo de Pago
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/metodoPago
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenCliente}}
}

body:json {
  {
    "stripePaymentMethodId": "pm_STRIPE_ID_AQUI"
  }
}
