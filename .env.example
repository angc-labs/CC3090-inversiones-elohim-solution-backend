JWT_KEY=
JWT_ISSUER=ElohimShop
JWT_AUDIENCE=ElohimShopAPI

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=
# También se puede usar Stripe__PublishableKey=pk_... (se une a la sección Stripe del configuration)
Stripe__PublishableKey=
Stripe__DefaultCurrency=gtq
# Opcional; si no se define, se usa Stripe__DefaultCurrency / appsettings
STRIPE_DEFAULT_CURRENCY=gtq

ConnectionStrings__DefaultConnection=