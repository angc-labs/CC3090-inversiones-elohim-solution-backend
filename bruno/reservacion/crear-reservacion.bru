meta {
  name: Crear Reservacion
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/reservaciones
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenCliente}}
}

body:json {
  {
    "metodoPagoId": "METODO_PAGO_ID_AQUI"
  }
}
