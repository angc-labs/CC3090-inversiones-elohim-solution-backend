meta {
  name: Crear Payment Intent
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/pagos/create-intent
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenCliente}}
}

body:json {
  {
    "reservacionId": "RESERVACION_ID_AQUI"
  }
}
